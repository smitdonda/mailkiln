name: Release

on:
  push:
    tags:
      - 'v*'

# `id-token: write` is what makes npm provenance possible: npm verifies the
# package was built by this workflow from this commit.
permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org
          cache: npm

      - run: npm ci

      - run: npm run typecheck
      - run: npm run lint
      - run: npm test
      - run: npm run build
      - run: npm run verify:types
      - run: npx publint
      # Through the npm script, not a bare npx call: the script passes
      # --ignore-rules no-resolution, and without it the CSS export (which has
      # no types by definition) and the node10 subpath fail the run.
      - run: npm run verify:attw

      - name: Check the tag matches package.json
        run: |
          TAG="${GITHUB_REF_NAME#v}"
          PKG=$(node -p "require('./package.json').version")
          if [ "$TAG" != "$PKG" ]; then
            echo "::error::tag $TAG does not match package.json version $PKG"
            exit 1
          fi

      # Trusted publishing (OIDC) needs npm >= 11.5.1; setup-node ships an older
      # npm with Node 22. No NODE_AUTH_TOKEN: the registry trades this workflow's
      # OIDC identity for a short-lived credential, so no long-lived token exists
      # to leak. Provenance comes with it automatically.
      - run: npm install -g npm@latest

      - run: npm publish --access public
